<div class="loading-modal @(IsLoading ? "show" : "")">
    <Spinner></Spinner>
</div>

@code {

    [Parameter]
    public bool IsLoading { get; set; }
    [Parameter]
    public EventCallback<bool> IsLoadingChanged { get; set; }

}
